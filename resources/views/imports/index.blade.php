@extends('layouts.app')
@section('title', __('Import data')) @section('page-title', __('Import data')) @section('page-subtitle', __('Guided CSV/XLSX import with preview and column mapping'))
@section('content')<div class="row g-4"><div class="col-xl-5"><div class="card fm-card"><div class="card-body"><form method="POST" action="{{ route('imports.preview') }}" enctype="multipart/form-data">@csrf<h5>{{ __('Start import') }}</h5><div class="mb-3"><label class="form-label">{{ __('Destination') }}</label><select name="resource_type" class="form-select">@foreach(array_keys($resources) as $resource)<option value="{{ $resource }}">{{ __(ucfirst($resource)) }}</option>@endforeach</select></div><div class="mb-3"><label class="form-label">{{ __('CSV or XLSX') }}</label><input type="file" name="file" accept=".csv,.xlsx,text/csv,application/vnd.openxmlformats-officedocument.spreadsheetml.sheet" required class="form-control"></div><button class="btn btn-primary"><i class="bi bi-upload me-1"></i>{{ __('Preview import') }}</button></form></div></div></div><div class="col-xl-7"><div class="card fm-card"><div class="card-header bg-white fw-semibold">{{ __('Recent imports') }}</div><div class="table-responsive"><table class="table fm-table mb-0"><thead><tr><th>{{ __('Module') }}</th><th>{{ __('Rows') }}</th><th>{{ __('Imported') }}</th><th>{{ __('Failed') }}</th><th>{{ __('Date') }}</th></tr></thead><tbody>@forelse($runs as $run)<tr><td>{{ ucfirst($run->resource_type) }}</td><td>{{ $run->total_rows }}</td><td class="text-success">{{ $run->imported_rows }}</td><td class="text-danger">{{ $run->failed_rows }}</td><td>{{ $run->created_at->format('d/m/Y H:i') }}</td></tr>@empty<tr><td colspan="5" class="text-center py-5">{{ __('No imports executed.') }}</td></tr>@endforelse</tbody></table></div></div></div></div>@endsection