@extends('layouts.app')
@section('title', __('API tokens')) @section('page-title', __('API tokens')) @section('page-subtitle', __('Create revocable credentials for external integrations'))
@section('content')<div class="row g-4"><div class="col-xl-4"><div class="card fm-card"><div class="card-body"><form method="POST" action="{{ route('integrations.api.store') }}">@csrf<h5>{{ __('Create token') }}</h5><div class="mb-3"><label class="form-label">{{ __('Name') }}</label><input class="form-control" name="name" required></div><div class="mb-3"><label class="form-label">{{ __('Expires at') }}</label><input class="form-control" type="date" name="expires_at"></div><div class="mb-3"><label class="form-label d-block">{{ __('Abilities') }}</label><div class="form-check form-check-inline"><input class="form-check-input" type="checkbox" name="abilities[]" value="read" id="ability-read" checked><label class="form-check-label" for="ability-read">Read</label></div><div class="form-check form-check-inline"><input class="form-check-input" type="checkbox" name="abilities[]" value="write" id="ability-write"><label class="form-check-label" for="ability-write">Write</label></div></div><div class="alert alert-warning small">{{ __('The complete token is shown only once after creation.') }}</div><button class="btn btn-primary w-100">{{ __('Generate token') }}</button></form></div></div></div><div class="col-xl-8"><div class="card fm-card"><div class="table-responsive"><table class="table fm-table mb-0"><thead><tr><th>{{ __('Name') }}</th><th>{{ __('Prefix') }}</th><th>{{ __('Last used') }}</th><th>{{ __('Expires') }}</th><th></th></tr></thead><tbody>@forelse($tokens as $token)<tr><td>{{ $token->name }}</td><td><code>{{ $token->token_prefix }}…</code></td><td>{{ $token->last_used_at?->diffForHumans() ?: '—' }}</td><td>{{ $token->expires_at?->format('d/m/Y') ?: '—' }}</td><td><form method="POST" action="{{ route('integrations.api.destroy',$token) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">{{ __('Revoke') }}</button></form></td></tr>@empty<tr><td colspan="5" class="text-center py-5">{{ __('No API tokens.') }}</td></tr>@endforelse</tbody></table></div></div></div></div>@endsection