@extends('layouts.app')
@section('title', __('Webhooks')) @section('page-title', __('Webhooks')) @section('page-subtitle', __('Send FlowManager events to external systems'))
@section('content')<div class="row g-4"><div class="col-xl-4"><div class="card fm-card"><div class="card-body"><form method="POST" action="{{ route('integrations.webhooks.store') }}">@csrf<h5>{{ __('New webhook') }}</h5><div class="mb-3"><label class="form-label">{{ __('Name') }}</label><input name="name" required class="form-control"></div><div class="mb-3"><label class="form-label">URL</label><input name="url" required type="url" class="form-control"></div><label class="form-label">{{ __('Events') }}</label>@foreach(['created','updated','deleted','task.completed','ticket.resolved'] as $event)<div class="form-check"><input type="checkbox" name="events[]" value="{{ $event }}" class="form-check-input" id="wh-{{ $loop->index }}"><label class="form-check-label" for="wh-{{ $loop->index }}">{{ $event }}</label></div>@endforeach<button class="btn btn-primary w-100 mt-3">{{ __('Create webhook') }}</button></form></div></div></div><div class="col-xl-8"><div class="card fm-card"><div class="table-responsive"><table class="table fm-table mb-0"><thead><tr><th>{{ __('Name') }}</th><th>URL</th><th>{{ __('Events') }}</th><th>{{ __('Deliveries') }}</th><th></th></tr></thead><tbody>@forelse($webhooks as $webhook)<tr><td>{{ $webhook->name }}</td><td class="text-break"><code>{{ $webhook->url }}</code></td><td>{{ implode(', ',$webhook->events) }}</td><td>{{ $webhook->deliveries_count }}</td><td><form method="POST" action="{{ route('integrations.webhooks.destroy',$webhook) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger"><i class="bi bi-trash"></i></button></form></td></tr>@empty<tr><td colspan="5" class="text-center py-5">{{ __('No webhooks configured.') }}</td></tr>@endforelse</tbody></table></div></div></div></div>@endsection