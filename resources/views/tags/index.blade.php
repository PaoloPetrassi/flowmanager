@extends('layouts.app')
@section('title', __('Tags')) @section('page-title', __('Tags')) @section('page-subtitle', __('Reusable labels across CRM and operations'))
@section('content')<div class="row g-4"><div class="col-lg-4"><div class="card fm-card"><div class="card-body"><form method="POST" action="{{ route('tags.store') }}">@csrf<h5>{{ __('New tag') }}</h5><div class="mb-3"><label class="form-label">{{ __('Name') }}</label><input name="name" required class="form-control"></div><div class="mb-3"><label class="form-label">{{ __('Color') }}</label><input name="color" class="form-control" placeholder="#2563eb"></div><button class="btn btn-primary w-100">{{ __('Create tag') }}</button></form></div></div></div><div class="col-lg-8"><div class="card fm-card"><div class="table-responsive"><table class="table fm-table mb-0"><thead><tr><th>{{ __('Tag') }}</th><th>{{ __('Usage') }}</th><th></th></tr></thead><tbody>@forelse($tags as $tag)<tr><td><span class="fm-tag" style="--tag-color:{{ $tag->color ?: '#64748b' }}">{{ $tag->name }}</span></td><td>{{ $tag->companies_count+$tag->projects_count+$tag->tasks_count+$tag->tickets_count }}</td><td class="text-end"><form method="POST" action="{{ route('tags.destroy',$tag) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger"><i class="bi bi-trash"></i></button></form></td></tr>@empty<tr><td colspan="3" class="text-center py-5">{{ __('No tags yet.') }}</td></tr>@endforelse</tbody></table></div></div></div></div>@endsection